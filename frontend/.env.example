# Base URL of the Node.js API gateway (api/). Defaults to
# http://localhost:3000 if unset -- only needed here to point at a
# different gateway (docker compose, a staging deployment, etc).
VITE_API_BASE_URL=http://localhost:3000
